"""
문제

N개의 정수가 주어진다. 이때, 최솟값과 최댓값을 구하는 프로그램을 작성하시오.
입력

첫째 줄에 정수의 개수 N (1 ≤ N ≤ 1,000,000)이 주어진다. 둘째 줄에는 N개의 정수를 공백으로 구분해서 주어진다. 모든 정수는 -1,000,000보다 크거나 같고, 1,000,000보다 작거나 같은 정수이다.
출력

첫째 줄에 주어진 정수 N개의 최솟값과 최댓값을 공백으로 구분해 출력한다.
"""

numOfArr = int(input())  # 입력 개수를 정수로 변환
Arr = list(map(int, input().split()))  # 입력된 정수들을 리스트로 변환

print(min(Arr))  # 최솟값 출력
print(max(Arr))  # 최댓값 출력


"""
챗GPT : 

max(Arr)는 리스트 Arr에 원소가 존재하는 경우에도 문제 없이 동작할 것입니다. 그러나 min(Arr)에서 문제가 발생하는 이유는, 코드에서 Arr을 두 번 사용하여 map() 함수로부터 얻은 map 객체를 두 번 소모하게 되기 때문입니다. map 객체는 이터레이터(iterator)로서, 한 번 소모하면 그 이후에 더 이상 사용할 수 없습니다.

따라서 Arr을 한 번만 사용하여 리스트로 변환한 후, 그 리스트를 min()과 max() 함수에 각각 넘겨주어야 합니다. 아래는 수정된 코드 예시입니다:
"""